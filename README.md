# 微机教室在线考试系统

面向微机教室局域网环境的在线考试系统。教师机在教室中作为服务器，学生机通过浏览器登录答题。

## 功能特性

- **题型支持**：选择题、判断题
- **学生端**：登录、在线答题（倒计时）、查看个人成绩与答题详情
- **教师端**：学生管理、考试管理（自定义题目）、发布/结束考试
- **成绩查询**：教师查看全班成绩，学生查看个人成绩
- **自动统计**：考试结束后自动批改并生成统计分析（平均分、及格率、分数段分布、各题正确率）
- **精美界面**：Vue 3 + Element Plus 现代化 UI

## 技术栈

| 层级 | 技术 |
|------|------|
| 后端 | Python FastAPI + SQLAlchemy |
| 数据库 | SQLite（无需额外安装） |
| 前端 | Vue 3 + Vite + Element Plus + Pinia |
| 认证 | JWT Token |

## 快速开始

### 环境要求

- Python 3.10+
- Node.js 18+（仅构建前端时需要）

### 一键启动

```bash
# 1. 安装后端依赖
pip install -r backend/requirements.txt

# 2. 构建前端
cd frontend && npm install && npm run build && cd ..

# 3. 启动服务器（教师机运行）
python run.py
```

启动后终端会显示访问地址：
- 教师机本地：`http://localhost:8000`
- 学生机访问：`http://<教师机IP>:8000`

### 默认账号

| 角色 | 用户名 | 密码 |
|------|--------|------|
| 教师 | teacher | teacher123 |
| 学生 | student01 ~ student05 | 123456 |

## 使用流程

### 教师操作

1. 使用教师账号登录
2. 在「学生管理」中添加/管理学生账号
3. 在「考试管理」→「创建考试」中自定义题目（选择题/判断题）
4. 点击「发布」开放考试
5. 考试结束后点击「结束」，系统自动批改并统计
6. 查看「统计」和「成绩」了解考试情况

### 学生操作

1. 使用学生账号登录
2. 在「考试大厅」选择进行中的考试
3. 答题并提交（时间到自动提交）
4. 在「我的成绩」查看分数和答题详情

## 项目结构

```
├── backend/                # FastAPI 后端
│   ├── app/
│   │   ├── main.py         # 应用入口
│   │   ├── models.py       # 数据库模型
│   │   ├── schemas.py      # 数据校验
│   │   ├── auth.py         # 认证模块
│   │   └── routers/        # API 路由
│   ├── seed.py             # 数据库初始化
│   └── requirements.txt
├── frontend/               # Vue 3 前端
│   ├── src/
│   │   ├── views/          # 页面组件
│   │   ├── layouts/        # 布局组件
│   │   ├── api/            # API 封装
│   │   └── stores/         # 状态管理
│   └── package.json
├── run.py                  # 启动脚本
└── README.md
```

## 开发模式

前后端分离开发：

```bash
# 终端 1：启动后端
cd backend && uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# 终端 2：启动前端开发服务器
cd frontend && npm run dev
```

前端开发服务器会自动代理 API 请求到后端。

## 局域网部署说明

1. 确保教师机防火墙允许 8000 端口入站
2. 学生机浏览器访问 `http://教师机IP:8000`
3. 所有数据存储在教师机的 `backend/exam_system.db` 文件中

## 后续扩展

当前版本已实现核心功能框架，后续可扩展：

- 填空题、简答题
- 防作弊（切屏检测、题目乱序）
- 批量导入学生/题目
- 成绩单导出（Excel/PDF）
- 多班级管理
